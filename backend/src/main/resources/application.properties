spring.application.name=SariSmart Backend API

# Server Configuration
# The server port can be set using the PORT environment variable
server.port=${PORT:8080}

# PostgreSQL Session Pooler config
spring.datasource.url=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.mpvwyygeoesopxralxxl
spring.datasource.password=SARISMARTfamily041125!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings
spring.datasource.hikari.maximum-pool-size=5
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=trues
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect